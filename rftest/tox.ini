[tox]
envlist = py27,py31
[testenv]
changedir=tests
deps= -r{toxinidir}/test-requirements.txt
commands=
  find . -type f -name "*.pyc" -delete
  py.test \
    --basetemp={envtmpdir}  
# py.test tempdir setting
#    {posargs} # substitute with tox' positional arguments
